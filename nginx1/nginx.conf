events {}

http {
    server {
        listen 80;

        location / {
            proxy_set_header X-Forwarded-For "$remote_addr, $server_addr";
            proxy_pass http://nginx2/internal;
        }
    }
}
